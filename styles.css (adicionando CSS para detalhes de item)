/* ... existing code ... */

/* Item Details Styles */
.item-details {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
}

.detail-row {
    display: flex;
    gap: 2rem;
}

.detail-group {
    flex: 1;
}

.detail-group h4 {
    font-size: 0.9rem;
    color: var(--gray-color);
    margin-bottom: 0.3rem;
}

.detail-group p {
    font-size: 1.1rem;
    color: var(--dark-color);
}

.detail-section {
    border-top: 1px solid #eee;
    padding-top: 1.5rem;
}

.detail-section h3 {
    font-size: 1.1rem;
    color: var(--primary-color);
    margin-bottom: 1rem;
}

.detail-footer {
    color: var(--gray-color);
    font-size: 0.9rem;
    text-align: right;
    margin-top: 1rem;
}

/* Report Preview Styles */
.report-preview {
    display: flex;
    flex-direction: column;
    gap: 1rem;
}

.report-summary, .report-recommendations {
    background-color: #f9f9f9;
    padding: 1rem;
    border-radius: 4px;
    margin-top: 1rem;
}

.report-summary h4, .report-recommendations h4 {
    color: var(--primary-color);
    margin-bottom: 0.5rem;
}

.report-note {
    margin-top: 1.5rem;
    color: var(--success-color);
    font-weight: 500;
    text-align: center;
}

/* ... existing code ... */

